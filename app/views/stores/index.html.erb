<div class="container">
  <section class="row">
    <div class="mt-2">
      <%= render "search_form", q: @q %>
    </div>
  </section>
  <section class="row">
    <div class="col-12 justify-content-center d-flex mb-5 mt-4">
      <h1 class="title-border h2 px-4 pb-2"><i class="fas fa-store-alt"></i>みんなが投稿してくれた施設の一覧</h1>
    </div>
  </section>
  <% if @stores.present? %>
    <div id ="paginate1" class="paginate-1 mb-3">
      <%= render "shared/paginate", pages: @stores, page_name: "page" %>
    </div>
    <div id="stores_list">
      <%= render "shared/stores_list", stores: @stores %>
    </div>
    <div id ="paginate2" class="paginate-2 mt-3">
      <%= render "shared/paginate", pages: @stores, page_name: "page" %>
    </div>
  <% else %>
    <%= render "no_store" %>
  <% end %>
</div>
